apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
# ARO-specific patches can be added here.
# Example: override storage class for ARO's default managed-premium
# patches:
#   - path: patches/storage-class.yaml
#     target:
#       kind: PersistentVolumeClaim
